# modelprep-prototype — moved

This repo has been folded into a monorepo. Active development is at:

→ **https://github.com/iamdjem/modelprep**

The new layout puts the frontend (this code) under `deploy/`, alongside the Cloudflare Worker backend and the CDN. Same product, one repo. Full git history of this prototype is preserved in the new repo under `deploy/`.

**This repo is archived.** No new commits will land here. The GitHub Pages site that previously deployed from this repo (https://iamdjem.github.io/modelprep-prototype/) still serves the last build for backwards compatibility, but the live ModelPrep app is now at https://iamdjem.github.io/modelprep/.
